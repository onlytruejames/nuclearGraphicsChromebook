Clone this into the instance of nucleargraphics you want converted, rename the folder to "camera", then run build.py, then run server.py, then find the address of the server, then run nuclearGraphics and input that address when it asks you, go to that address and follow 